<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CMPUT301_APP">

        <!-- Core Activities -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".DeleteProfileActivity"
            android:exported="false" />

        <!-- Entrant Activities -->
        <activity
            android:name=".entrant.DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".entrant.EventDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".entrant.ScanQRActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".entrant.MyEventsActivity"
            android:exported="false" />

        <!-- Organizer Activities -->
        <activity
            android:name=".organizer.OrganizerDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".organizer.CreateEventActivity"
            android:exported="false" />
        <activity
            android:name=".organizer.OrganizerEventDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".organizer.QRDisplayActivity"
            android:exported="false" />
        <activity
            android:name=".organizer.RegistrationTimingActivity"
            android:exported="false" />
        <activity
            android:name=".organizer.EntrantListActivity"
            android:exported="false" />
        <activity
            android:name=".organizer.LotteryDrawActivity"
            android:exported="false" />

    </application>

</manifest>
